import { makeStyles } from "@mui/styles";

export default makeStyles((theme)=>({
    img:{
        maxWidth:'90%',
        borderRadius:'20px',
        objectFit:'cover',
        boxShadow:'0.7rem 0.7rem 1rem'
    }
}))